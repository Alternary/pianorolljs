simple piano roll sequencer and sampler in javascript
run "npm run backend" to start up backend and "npm run dev" to start up frontend
currently samples is excluded from repo, so you'll have to add your own samples
